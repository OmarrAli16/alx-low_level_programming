#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print z-a
 *
 * Return: always 0 (success)
 */

int main(void)
{
	char last = 'z';

	while (last >= a)
	{
		putchar(last);
		last--;
	}
	putchar('\n');

	return (0);
}
